apiVersion: v1
kind: Service
metadata:
  name: "wordpress"
  namespace: wordpress
spec:
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 3306
      targetPort: 3306
      nodePort: 30332
  selector:
    app: "wordpress"