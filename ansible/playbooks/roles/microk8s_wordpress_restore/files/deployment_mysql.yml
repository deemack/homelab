---
apiVersion: v1
kind: Service
metadata:
  name: mysql_restore
  namespace: wordpress_restore
spec:
  selector:
    app: mysql_restore
  ports:
  - name: mysql_restore
    protocol: TCP
    port: 3306
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql_restore
  namespace: wordpress_restore
  labels:
    app: mysql_restore
spec:
  selector:
    matchLabels:
      app: mysql_restore
  template:
    metadata:
      labels:
        app: mysql_restore
    spec:
      containers:
      - image: mysql:8.0
        name: mysql_restore
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret_restore
              key: ROOT_PASSWORD
        - name: MYSQL_DATABASE
          valueFrom:
            secretKeyRef:
              name: mysql-secret_restore
              key: DATABASE
        - name: MYSQL_USER
          valueFrom:
            secretKeyRef:
              name: mysql-secret_restore
              key: USER
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret_restore
              key: PASSWORD
        ports:
        - containerPort: 3306
          name: mysql_restore_restore
        volumeMounts:
        - name: mysql-persistent-storage_restore
          mountPath: /var/lib/mysql
      volumes:
      - name: mysql-persistent-storage_restore
        persistentVolumeClaim:
          claimName: mysql-pv-claim_restore