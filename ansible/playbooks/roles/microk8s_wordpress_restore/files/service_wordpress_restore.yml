apiVersion: v1
kind: Service
metadata:
  name: "wordpress_restore"
  namespace: wordpress-restore
spec:
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30333
  selector:
    app: "wordpress_restore"