apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-restore
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/storage/wordpress-restore/mysql-restore"