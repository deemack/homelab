apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: handbrake-pv-claim-config
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi