apiVersion: v1
kind: Service
metadata:
  name: "wikijs"
  namespace: wikijs-ns
spec:
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30330
  selector:
    app: "wikijs"
